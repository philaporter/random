# Random with Go

* health check handling
* graceful shutdown things
* http server with gorilla/mux
* dockerfile defined

## How to run

* `docker build -t random .`
* `docker run random`

OR

* `dep ensure`
* `go build`
* `./random`